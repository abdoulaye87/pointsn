export default function SlugLayout({
  children,
}: {
  children: React.ReactNode
}) {
  // Layout minimal pour les pages de sites générés
  // Pas de styles globaux, pas de polices spéciales
  // Le contenu (site généré ou page d'attente) gère son propre style
  return children
}
